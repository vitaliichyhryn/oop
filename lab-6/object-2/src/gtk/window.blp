using Gtk 4.0;
using Adw 1;

template $Window: Adw.ApplicationWindow {
  title: _("Об'єкт №2");
  default-width: 400;
  default-height: 600;

  Adw.ToolbarView toolbar_view {
    [top]
    Adw.HeaderBar {
    }
  }
}

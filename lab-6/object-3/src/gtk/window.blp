using Gtk 4.0;
using Adw 1;

template $Window: Adw.ApplicationWindow {
  title: _("Об'єкт №3");
  default-width: 800;
  default-height: 800;

  content: Adw.ToolbarView toolbar_view {
    [top]
    Adw.HeaderBar {
    }
  };
}
